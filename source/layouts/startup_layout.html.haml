!!!
%html
  %head
    %meta{:charset => "utf-8"}/
    / Always force latest IE rendering engine or request Chrome Frame
    %meta{:content => "IE=edge,chrome=1", "http-equiv" => "X-UA-Compatible"}/
    / Use title if it's in the page YAML frontmatter
    %title= data.page.title + " - The Distiller"
    = stylesheet_link_tag 'http://fonts.googleapis.com/css?family=Roboto:400,100,300,700,100italic,300italic,400italic,700italic|Open+Sans:300italic,400italic,700italic,400,300,700'
    = stylesheet_link_tag "all"
    = javascript_include_tag  "all"
  %body{:class => page_classes}
    %header
      = partial 'shared/nav'
    .container.startup
      .header
        %h2= current_page.data.title
        = link_to "Contact Startup", current_page.data.contact_url

      .databox
        .databox-top
          %a{:href=>current_page.data.home_url, :target => '_blank'}
            = image_tag "#{current_page.path[0..-6]}/logo.png"
          = link_to current_page.data.home_url, current_page.data.home_url
          %table
            %tr
              %td.left Date Started
              %td.right= current_page.data.date_started
        .databox-bottom
          %p Team members
          %ul
            - current_page.data.founders.each do |f|
              %li= f
      .headimg
        = image_tag "#{current_page.path[0..-6]}/banner.jpg"
      .content
        = yield
      %footer
        = partial 'shared/startup_footer'
